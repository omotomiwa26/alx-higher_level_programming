#!/usr/bin/python3
"""
    This module uses the numpy library for matrix multiplication
"""
import numpy


def lazy_matrix_mul(m_a, m_b):
    """
        Defining the two matrices m_a and m_b
    """

    return numpy.matmul(m_a, m_b)
